services:
    app:
        build:
            context: .
            target: production
        restart: unless-stopped
        env_file:
            - .env
        command: npm start
        networks:
            - weather

networks:
    weather:
        name: weather
